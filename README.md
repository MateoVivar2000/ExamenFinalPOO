💣 Buscaminas en Consola (Minesweeper CLI)Proyecto final para la asignatura de Programación Orientada a Objetos (POO), Universidad Politécnica Salesiana. Implementación completa del clásico juego Buscaminas en una interfaz de consola. El desarrollo se enfocó en la aplicación estricta de principios de POO, Patrón MVC y técnicas de Código Limpio (DRY, KISS).🚀 Características y Principios TécnicosArquitectura MVC (Modelo-Vista-Controlador): Implementación estricta para desacoplar la lógica de negocio (modelo) de la presentación (vista) y el flujo de la aplicación (controlador).Algoritmos de Juego: Implementación del algoritmo de expansión recursiva de casillas vacías (Flood Fill) dentro de la clase Tablero.Persistencia de Datos: Uso de la interfaz Serializable para guardar y cargar el estado completo de la partida mediante serialización binaria.Manejo de Errores Avanzado: Utilización de excepciones personalizadas (ej. CasillaYaDescubiertaException) para manejar errores de la lógica interna del juego de forma clara y semántica, separando esta responsabilidad del Controlador.Código Limpio (KISS/SRP): Las clases cumplen el Principio de Responsabilidad Única (SRP); por ejemplo, VistaConsola solo se ocupa de la E/S y el formateo, sin lógica de juego.🛠️ Tecnologías y RequisitosLenguaje: Java SE 21 (o superior).Entorno de Desarrollo: Cualquier IDE moderno compatible con Java (Eclipse, IntelliJ IDEA, VS Code).Testing: JUnit 5 (para pruebas unitarias).⚙️ Guía para Duplicar y Configurar el ProyectoEsta sección detalla los pasos para obtener el código fuente y configurarlo para su ejecución y desarrollo en un IDE.1. Obtener el Código FuenteEl método recomendado para duplicar el proyecto es clonar el repositorio de GitHub:Bashgit clone https://github.com/MateoVivar2000/UNIDAD 4.git
cd NombreDelDirectorio
2. Configuración en el IDE (Eclipse, IntelliJ, etc.)El proyecto utiliza una estructura de paquetes estándar, que facilita su importación:Abrir Proyecto: En tu IDE, selecciona "Importar Proyecto Existente" o "Abrir" y navega hasta el directorio raíz del proyecto (ExamenPractico).Configurar Build Path/Dependencias: Asegúrate de que el proyecto utilice Java SE 21 como su JRE/SDK.Configurar Paquetes Fuente: Verifica que los directorios src/controlador, src/modelo, src/vista, y src/main estén configurados como Carpetas de Código Fuente (Source Folders) de Java.3. Estructura de Paquetes y ClasesEl código está dividido en cuatro paquetes principales, siguiendo la convención de paquetes de Java y el patrón MVC:PaqueteClases ClaveResponsabilidad PrimariamainMain.javaInicializa el sistema, actúa como el bootstrapper del MVC.controladorControladorJuego.javaGestiona el flujo del juego, procesa la entrada de comandos y maneja excepciones del sistema.modeloJuegoBuscaminas.java, Tablero.java, Casilla.javaLógica de negocio, estado del tablero, reglas de juego, y serialización.vistaVistaConsola.javaEntrada/Salida de datos, impresión del tablero y formateo de mensajes.4. EjecuciónUna vez configurado, ejecuta la clase principal: main.Main.java. El IDE generará el bytecode necesario para iniciar la aplicación.🎮 Modo de UsoUna vez que inicies el juego, se te pedirá ingresar comandos en el formato [ACCIÓN] [COORDENADA].AcciónFormato de EntradaDescripciónDescubrirD A5Revela el contenido de la casilla A5. Si es mina, el juego termina.Marcar/BanderaM B3Coloca o retira una bandera en la casilla B3.Coordenadas:Las filas van de A a J y las columnas van de 1 a 10.👥 Equipo y ContribucionesRosa SilvaMateo VivarAlexander BarragánPaulina Guadalupe
